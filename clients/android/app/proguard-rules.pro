# Add project specific ProGuard rules here.
# Default AGP rules already cover AndroidX/OkHttp reflection needs for this app's usage
# (no reflection-based JSON mapping - org.json is used directly). Nothing extra needed yet.
